{
  "name": "FrontDesk AI",
  "image": "mcr.microsoft.com/devcontainers/python:3.12-bookworm",

  // Minimum 4-core / 8GB RAM recommended: kind control plane (~500MB)
  // + Docker daemon (~200MB) + FrontDesk AI with ONNX/ChromaDB (~1GB) = ~2GB+
  "hostRequirements": {
    "cpus": 2,
    "memory": "4gb",
    "storage": "32gb"
  },

  "features": {
    // SSH server — enables `gh codespace ssh` and remote Claude Code access
    "ghcr.io/devcontainers/features/sshd:1": {
      "version": "latest"
    },
    // Docker-in-Docker so kind can spawn cluster nodes as containers
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "version": "latest",
      "moby": true
    },
    // kubectl + helm CLI
    "ghcr.io/devcontainers/features/kubectl-helm-minikube:1": {
      "version": "latest",
      "helm": "latest",
      "minikube": "none"
    }
  },

  "postCreateCommand": "bash .devcontainer/setup.sh",

  "forwardPorts": [8000, 9090, 3000, 5000, 4317],
  "portsAttributes": {
    "8000": { "label": "FrontDesk AI",       "onAutoForward": "openBrowser" },
    "9090": { "label": "Prometheus",          "onAutoForward": "notify" },
    "3000": { "label": "Grafana",             "onAutoForward": "notify" },
    "5000": { "label": "Docker Registry",     "onAutoForward": "silent" },
    "4317": { "label": "OTEL/Tempo (gRPC)",   "onAutoForward": "silent" }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        // Python
        "ms-python.python",
        "ms-python.vscode-pylance",
        // Kubernetes + Docker
        "ms-kubernetes-tools.vscode-kubernetes-tools",
        "ms-azuretools.vscode-docker",
        // YAML / shell / env files
        "redhat.vscode-yaml",
        "timonwong.shellcheck",
        "mikestead.dotenv",
        // Jinja2 templates (templates/login.html)
        "samuelcolvin.jinjahtml",
        // SQLite viewer (history.db, frontdesk_tools.db, checkpoints.db)
        "mtxr.sqliteviewer",
        // API testing
        "rangav.vscode-thunder-client",
        // Git
        "eamodio.gitlens"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.terminal.activateEnvironment": true,
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },

  "remoteEnv": {
    "KUBECONFIG": "/home/vscode/.kube/config",
    "SQLITE_DIR": "/shared/.sqlite"
  }
}
