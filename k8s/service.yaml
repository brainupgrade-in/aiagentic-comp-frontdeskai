apiVersion: v1
kind: Service
metadata:
  name: app
  labels:
    app: frontdeskai
spec:
  selector:
    app: frontdeskai
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
    - name: metrics
      protocol: TCP
      port: 9090
      targetPort: 8000
